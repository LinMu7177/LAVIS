 # Copyright (c) 2022, salesforce.com, inc.
 # All rights reserved.
 # SPDX-License-Identifier: BSD-3-Clause
 # For full license text, see the LICENSE file in the repo root or https://opensource.org/licenses/BSD-3-Clause

# Overall Accuracy is: 51.88 (result different from BLIP-2 paper due to different implementation and transformers version)

model:
  arch: blip2_vicuna_instruct
  model_type: vicuna7b
  use_grad_checkpoint: False
  llm_model: "/mnt/local/share/models/Vicuna/vicuna-7b/"

datasets:
  coco_vqa: # name of the dataset builder
    type: eval
    vis_processor:
        eval:
          name: "blip_image_eval"
          image_size: 224
    text_processor:
        eval:
          name: "blip_question"
    # build_info:
    #     images:
    #         storage: '/export/share/datasets/vision/coco/images/'

run:
  task: vqa
  # optimization-specific
  batch_size_train: 48
  batch_size_eval: 48
  num_workers: 0

  # inference-specific
  max_len: 10
  min_len: 1
  num_beams: 5
  inference_method: "generate"
  prompt: "Question: {} Short answer:"

  seed: 42
  output_dir: "/mnt/local/wwx/Output/LAVIS/InstructBLIP/VQAEval/test_demo"
  # output_dir: "/root/autodl-tmp/wwx/Output/LAVIS/InstructBLIP/VQAEval/test_debug"
  evaluate: True
  test_splits: ["val"]

  # distribution-specific
  device: "cuda"
  world_size: 4
  dist_url: "env://"
  distributed: True
